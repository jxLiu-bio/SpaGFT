version: 2

build:
  os: ubuntu-20.04
  apt_packages:
    - cmake
    - g++
  tools:
    python: "3.7"


sphinx:

  configuration:  source/conf.py

formats: all

python:
  # version: 3.7
  install:
    - requirements: source/requirements.txt