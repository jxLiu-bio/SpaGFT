version: 2

sphinx:
  configuration: source/conf.py
  builder: html

build:
  apt_packages:
    - cmake
    - g++

conda:
  environment: docs/environment.yml

python:
  version: "3.7"
  install:
    - method: pip
      path: .
      extra_requirements:
        - source
  system_packages: true
